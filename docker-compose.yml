services:
  postgres:
    image: postgres:15-alpine
    container_name: videoboard-db
    environment:
      POSTGRES_USER: videoboard
      POSTGRES_PASSWORD: videoboard
      POSTGRES_DB: videoboard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
